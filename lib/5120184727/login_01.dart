import 'package:flutter/material.dart';

class login_01 extends StatelessWidget{
  Widget build(BuildContext context){
    return Center(
      child:Text('01\'s homepage',
      textDirection: TextDirection.ltr,)
    );
  }
}